# CalendarWorld-Agent
Need help planning your day? This is an LLM agent that lives inside a simulated day-planning calendar world. The agent observes structured state (time, location, stress, deadlines) on a 100x100 grid, chooses actions from a strict action space, and gets validated by the environment. Includes mock + Claude agents with comparison mode.
